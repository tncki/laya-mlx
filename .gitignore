.venv/
# The upstream checkout CI clones. No trailing slash: a symlink to an existing checkout
# (`.upstream -> ../laya`) is not matched by the directory-only pattern and would otherwise
# be committed as a local absolute path.
.upstream
.cache/
models/
*.safetensors
__pycache__/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.DS_Store
.env
*.log
artifacts/
# Local agent-handoff note: a pointer to /Users/jack/workspace/LAYA-HANDOFF.md. Working state
# for whoever picks this up next, not project documentation, so it stays out of the repository.
HANDOFF.md
